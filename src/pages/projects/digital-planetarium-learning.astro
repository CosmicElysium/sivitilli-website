---
import BaseLayout from "../../layouts/BaseLayout.astro";
import ProjectImage from "../../components/ProjectImage.astro";
import { projects } from "../../data/content";

const project = projects.find(
  (item) => item.slug === "digital-planetarium-learning",
);

if (!project) {
  throw new Error(
    'Project "digital-planetarium-learning" was not found in src/data/content.ts.',
  );
}

const statusLabel = project.status === "past" ? "Completed" : "Current";

// These assets are extracted from figures in the doctoral thesis. Confirm that
// each remains suitable for public reuse before publishing the page.
const images = {
  hero: {
    src: "/images/projects/digital-planetarium/iziko-planetarium.webp",
    width: "1373",
    height: "815",
    alt: "Exterior of the Iziko South African Museum and Planetarium in Cape Town, with the planetarium dome rising above the entrance",
    caption:
      "The Iziko Planetarium & Digital Dome in Cape Town, where the study visits took place.",
  },
  development: {
    src: "/images/projects/digital-planetarium/show-development.webp",
    width: "712",
    height: "534",
    alt: "A curved multi-panel display in the IDIA and UCT Visualisation Laboratory showing a digital planetarium scene during development of the second student visit",
    caption:
      "Developing the second planetarium visit on the Cobra display in the IDIA/UCT Visualisation Laboratory.",
  },
  attentiveness: {
    src: "/images/projects/digital-planetarium/spectrum-of-attentiveness.webp",
    width: "901",
    height: "457",
    alt: "Graph showing relevant engagement peaking at an intermediate level of attentiveness, with boredom at low attentiveness and distraction at high attentiveness",
    caption:
      "The localised model of student engagement: relevant engagement was highest within an intermediate range of attentiveness.",
  },
  context: {
    src: "/images/projects/digital-planetarium/contextual-influence.webp",
    width: "1671",
    height: "1726",
    alt: "Diagram organising factors that shape a digital planetarium experience into content, environment, instructor and external sources",
    caption:
      "The localised model of contextual influence groups the interacting aspects of a planetarium experience by their source.",
  },
  model: {
    src: "/images/projects/digital-planetarium/mcdip.webp",
    width: "1377",
    height: "827",
    alt: "Diagram of the Model for Curriculum Design in the Planetarium linking instructional intent, planetarium aspects, student engagement, cognitive load and instructional outcome",
    caption:
      "The Model for Curriculum Design in the Planetarium connects instructional intent, planetarium aspects and student engagement with the intended learning outcome.",
  },
};

const researchFigures = [
  images.attentiveness,
  images.context,
  images.model,
].filter((image) => image.src);

const visitRows = [
  {
    phase: "SPV1",
    timing: "Course week 2",
    participants: "94",
    design:
      "A lecturer-planned visit covering the night sky, celestial sphere and several related course topics.",
    data:
      "Pre- and post-visit responses, experience ratings, observations and a follow-up instructor interview.",
  },
  {
    phase: "SPV2",
    timing: "Course week 11",
    participants: "75",
    design:
      "A researcher-designed distance and scale show using two visual representations within the digital dome.",
    data:
      "Written responses after each segment, enjoyment choices, content reflection and detailed observational notes.",
  },
];
---

<BaseLayout
  title="Characterising the Digital Planetarium as a Teaching and Learning Space · Alexander Sivitilli"
  description="Doctoral astronomy education research on student engagement, contextual influence and curriculum design in a digital planetarium."
>
  <article class="project-page">
    <nav class="project-breadcrumb shell" aria-label="Breadcrumb">
      <a href="/projects/">Projects</a>
      <span aria-hidden="true">/</span>
      <span aria-current="page">Digital planetarium learning</span>
    </nav>

    <header
      class:list={[
        "project-detail-hero",
        "shell",
        !images.hero.src && "project-detail-hero--without-media",
      ]}
    >
      <div class="project-detail-intro">
        <p class="eyebrow">Astronomy education research · Completed PhD project</p>
        <h1>Characterising the Digital Planetarium as a Teaching and Learning Space</h1>
        <p class="project-deck">
          A Grounded Theory investigation of how university students engage
          with a digital planetarium, what shapes the learning environment and
          how those findings can inform curriculum design.
        </p>

        <div class="button-row">
          {
            project.links.map((link) => (
              <a
                class:list={[
                  "button",
                  link.type === "Thesis" && "primary",
                ]}
                href={link.href}
                target="_blank"
                rel="noopener noreferrer"
              >
                {link.type === "Thesis" ? "Read the thesis" : "Read the paper"}
              </a>
            ))
          }
        </div>
      </div>

      {
        images.hero.src && (
          <figure class="project-hero-media">
            <ProjectImage
              src={images.hero.src}
              alt={images.hero.alt}
              width={images.hero.width}
              height={images.hero.height}
              loading="eager"
              fetchpriority="high"
            />
            <figcaption>{images.hero.caption}</figcaption>
          </figure>
        )
      }

      <dl class="project-facts">
        <div>
          <dt>Status</dt>
          <dd>{statusLabel}</dd>
        </div>
        <div>
          <dt>Dates</dt>
          <dd>{project.period}</dd>
        </div>
        <div>
          <dt>Institution</dt>
          <dd>{project.organisation}</dd>
        </div>
        <div>
          <dt>Research setting</dt>
          <dd>Iziko Planetarium & Digital Dome</dd>
        </div>
        <div>
          <dt>Role</dt>
          <dd>Doctoral researcher and planetarium show designer</dd>
        </div>
      </dl>

      <ul class="project-tags" aria-label="Project topics">
        {project.tags.map((tag) => <li>{tag}</li>)}
      </ul>
    </header>

    <div class="project-body shell">
      <section class="project-detail-section" aria-labelledby="overview-title">
        <div class="section-label">
          <p class="eyebrow">Overview</p>
          <h2 id="overview-title">Studying the dome as a complete learning environment.</h2>
        </div>

        <div class="project-prose">
          <p class="lead">
            Digital planetariums can present the night sky, virtual journeys and
            scientific data across a hemispherical display. Those capabilities
            create teaching opportunities that are difficult to reproduce on a
            classroom screen, but they also introduce a dense mixture of visual,
            social, environmental and instructional influences.
          </p>

          <p>
            Rather than asking only whether a planetarium is more effective than
            another medium, my doctoral work examined the teaching and learning
            space itself. The study followed university students during two
            visits to the Iziko Planetarium & Digital Dome that formed part of
            an introductory astronomy course at the University of Cape Town.
          </p>

          <p>
            Student responses and detailed observations were analysed using the
            Constructivist Grounded Theory Method. The resulting models connect
            what students attend to, the contextual factors surrounding an
            immersive lesson and the decisions an instructor makes when
            incorporating a planetarium into a curriculum.
          </p>
        </div>
      </section>

      <section class="project-detail-section" aria-labelledby="questions-title">
        <div class="section-label">
          <p class="eyebrow">Research questions</p>
          <h2 id="questions-title">Two enquiries anchored the investigation.</h2>
        </div>

        <div class="system-grid system-grid--two">
          <article class="system-card">
            <p class="system-number" aria-hidden="true">01</p>
            <h3>How do students engage with a digital planetarium?</h3>
            <p>
              Written responses and experience ratings were used to examine
              what students attended to, what they recalled and what patterns 
              emerged across each visit.
            </p>
          </article>

          <article class="system-card">
            <p class="system-number" aria-hidden="true">02</p>
            <h3>What shapes its teaching and learning space?</h3>
            <p>
              Observations of planning, presentation, software, the physical
              theatre and the wider visit were analysed to identify the factors
              that construct the experience around the student.
            </p>
          </article>
        </div>
      </section>

      <section class="project-detail-section" aria-labelledby="role-title">
        <div class="section-label">
          <p class="eyebrow">My role</p>
          <h2 id="role-title">Research design, show development and qualitative analysis.</h2>
        </div>

        <div class="project-prose">
          <p>
            I led the doctoral investigation from its broad exploratory phase
            through data collection, analysis and model development. The work
            combined astronomy education research with practical experience of
            designing content for an immersive digital dome.
          </p>

          <ul class="scope-list">
            <li>
              Developed the study design and student-response instruments with
              the UCT Physics & Astronomy Education Research group.
            </li>
            <li>
              Documented show planning, planetarium visits and an instructor
              interview as observational data, with ethics approval and
              anonymised participation procedures.
            </li>
            <li>
              Storyboarded, developed and presented the second student visit,
              including scripted camera movements and distance representations
              in DigitalSky-Dark Matter.
            </li>
            <li>
              Digitised and analysed questionnaire and observational data using
              NVivo, Python and iterative Grounded Theory coding.
            </li>
            <li>
              Developed the localised models and the Model for Curriculum Design
              in the Planetarium, then documented the work in the thesis and a
              peer-reviewed conference proceeding.
            </li>
          </ul>
        </div>
      </section>

      <section class="project-detail-section" aria-labelledby="approach-title">
        <div class="section-label">
          <p class="eyebrow">Approach</p>
          <h2 id="approach-title">An iterative investigation shaped by what emerged.</h2>
        </div>

        <div class="project-prose">
          <ol class="process-list">
            <li>
              <div>
                <h3>Familiarise with the facility and frame the problem</h3>
                <p>
                  Exploratory visits, background research and discussion with
                  the research group established the digital planetarium as a
                  complex setting rather than a single instructional tool.
                </p>
              </div>
            </li>
            <li>
              <div>
                <h3>Observe the first student visit</h3>
                <p>
                  The course lecturer planned and presented the first show. I
                  recorded the planning process, administered pre- and post-show
                  questionnaires and documented how the visit unfolded.
                </p>
              </div>
            </li>
            <li>
              <div>
                <h3>Code early findings and refine the next phase</h3>
                <p>
                  Initial coding highlighted instructional intent, environmental
                  framing and the distinction between learning-related and
                  novelty-related engagement. Those findings informed the second
                  visit and its revised response instrument.
                </p>
              </div>
            </li>
            <li>
              <div>
                <h3>Design and run a focused second show</h3>
                <p>
                  I created a two-segment demonstration of astronomical distance
                  and scale, using the digital dome's virtual camera to compare
                  different spatial representations of the same content.
                </p>
              </div>
            </li>
            <li>
              <div>
                <h3>Build and connect the emergent models</h3>
                <p>
                  Student and observational data produced two localised models.
                  Working Memory and Cognitive Load Theory were then used as
                  theoretical supplements to connect them into MCDiP.
                </p>
              </div>
            </li>
          </ol>
        </div>
      </section>

      <section class="project-detail-section" aria-labelledby="visits-title">
        <div class="section-label">
          <p class="eyebrow">Study phases</p>
          <h2 id="visits-title">Two visits with the same introductory astronomy cohort.</h2>
        </div>

        <div class="project-prose project-prose--wide">
          <div
            class="table-wrap"
            tabindex="0"
            aria-label="Scrollable comparison of the two student planetarium visits"
          >
            <table>
              <caption>
                The participant counts and activities describe this particular
                University of Cape Town cohort and should not be read as general
                measures of planetarium performance.
              </caption>
              <thead>
                <tr>
                  <th scope="col">Phase</th>
                  <th scope="col">Timing</th>
                  <th scope="col">Participants</th>
                  <th scope="col">Planetarium instruction</th>
                  <th scope="col">Data collected</th>
                </tr>
              </thead>
              <tbody>
                {
                  visitRows.map((row) => (
                    <tr>
                      <th scope="row">{row.phase}</th>
                      <td>{row.timing}</td>
                      <td>{row.participants}</td>
                      <td>{row.design}</td>
                      <td>{row.data}</td>
                    </tr>
                  ))
                }
              </tbody>
            </table>
          </div>
        </div>
      </section>

      <section class="project-detail-section" aria-labelledby="show-title">
        <div class="section-label">
          <p class="eyebrow">Planetarium show design</p>
          <h2 id="show-title">Representing astronomical distance from two perspectives.</h2>
        </div>

        <div>
          <div class="project-prose">
            <p class="lead">
              The second visit focused on a single instructional intent: to give
              students a multi-perspective visualisation that improved their
              sense of scale for objects and distances encountered in
              introductory astronomy.
            </p>
          </div>

          <div class="system-grid system-grid--two system-grid--spaced">
            <article class="system-card system-card--compact">
              <p class="system-number" aria-hidden="true">01</p>
              <h3>Relative-scale zoom</h3>
              <p>
                The virtual camera moved away from Earth perpendicular to the
                path between milestone objects. Concentric distance circles
                expanded by powers of ten, with the unit changing from familiar
                local scales to larger astronomical references.
              </p>
            </article>

            <article class="system-card system-card--compact">
              <p class="system-number" aria-hidden="true">02</p>
              <h3>Fly-through perspective</h3>
              <p>
                The camera travelled along the same path while remaining
                oriented towards Earth. Objects entered the dome from behind
                the audience and a numerical counter replaced the concentric
                distance grid.
              </p>
            </article>
          </div>

          {
            images.development.src && (
              <figure class="project-wide-media">
                <ProjectImage
                  src={images.development.src}
                  alt={images.development.alt}
                  width={images.development.width}
                  height={images.development.height}
                  loading="lazy"
                />
                <figcaption>{images.development.caption}</figcaption>
              </figure>
            )
          }
        </div>
      </section>

      <section class="project-detail-section" aria-labelledby="findings-title">
        <div class="section-label">
          <p class="eyebrow">Key findings</p>
          <h2 id="findings-title">Engagement depends on more than immersion alone.</h2>
        </div>

        <div>
          <div class="outcomes-grid outcomes-grid--three">
            <article>
              <h3>Spectrum of attentiveness</h3>
              <p>
                Student engagement suggested an intermediate range in which
                attention was sufficient and directed towards the intended
                content. Too little attention appeared as boredom; too many
                competing stimuli appeared as distraction.
              </p>
            </article>

            <article>
              <h3>Contextual influence</h3>
              <p>
                The planetarium experience was shaped by interacting aspects
                originating in the content, physical environment, instructor
                and external context. The dome was one part of a much larger
                teaching and learning system.
              </p>
            </article>

            <article>
              <h3>Curriculum-design model</h3>
              <p>
                The two localised models were connected with Working Memory and
                Cognitive Load Theory to form MCDiP, a structured way to relate
                instructional intent, planetarium design choices and learning
                outcomes.
              </p>
            </article>
          </div>

          <aside class="evidence-note" aria-labelledby="evidence-title">
            <p class="eyebrow">One signal from the second visit</p>
            <h3 id="evidence-title">Engagement shifted as the show progressed.</h3>
            <p>
              Eighty per cent of students indicated that they enjoyed the first
              segment, compared with 61% for the second. Their written responses
              also shifted away from specific learning-related positives towards
              general novelty and descriptions of repetition or blandness. This
              was one input to the attentiveness model, not a stand-alone measure
              of learning or a general result for digital planetariums.
            </p>
          </aside>
        </div>
      </section>

      <section class="project-detail-section" aria-labelledby="model-title">
        <div class="section-label">
          <p class="eyebrow">MCDiP in practice</p>
          <h2 id="model-title">Turning an instructional goal into design decisions.</h2>
        </div>

        <div>
          <div class="project-prose">
            <p>
              MCDiP begins with the outcome an instructor wants students to
              construct. It then separates the planetarium experience into three
              classes of aspects so that the unique value of the dome is not
              confused with everything else that happens during a visit.
            </p>
          </div>

          <div class="system-grid system-grid--spaced">
            <article class="system-card system-card--compact">
              <p class="system-number" aria-hidden="true">01</p>
              <h3>Primary aspects</h3>
              <p>
                The affordances that justify using the planetarium for a
                particular goal, such as the full dome or a virtual camera that
                can present spatial relationships unavailable on a flat screen.
              </p>
            </article>

            <article class="system-card system-card--compact">
              <p class="system-number" aria-hidden="true">02</p>
              <h3>Secondary aspects</h3>
              <p>
                Elements that are reasonably controlled by the instructor but
                are not the central reason for using the facility, including
                pacing, labels, music, seating and the sequence of a show.
              </p>
            </article>

            <article class="system-card system-card--compact">
              <p class="system-number" aria-hidden="true">03</p>
              <h3>Tertiary aspects</h3>
              <p>
                Conditions outside the instructor's reasonable control, such as
                the planetarium's location, room layout, available hardware,
                software constraints and other environmental interruptions.
              </p>
            </article>
          </div>
        </div>
      </section>

      {
        researchFigures.length > 0 && (
          <section class="project-detail-section" aria-labelledby="figures-title">
            <div class="section-label">
              <p class="eyebrow">Research figures</p>
              <h2 id="figures-title">From student engagement to curriculum design.</h2>
            </div>

            <div class="model-gallery">
              {researchFigures.map((image) => (
                <figure>
                  <div class="diagram-frame">
                    <ProjectImage
                      src={image.src}
                      alt={image.alt}
                      width={image.width}
                      height={image.height}
                      loading="lazy"
                    />
                  </div>
                  <figcaption>{image.caption}</figcaption>
                </figure>
              ))}
            </div>
          </section>
        )
      }

      <section class="project-detail-section" aria-labelledby="outcomes-title">
        <div class="section-label">
          <p class="eyebrow">Outcomes</p>
          <h2 id="outcomes-title">A documented study and a framework for further testing.</h2>
        </div>

        <div class="outcomes-grid">
          <article>
            <h3>Doctoral thesis</h3>
            <p>
              The completed thesis provides the full account of the research
              design, student visits, qualitative analysis, emergent models and
              implications for instruction in a digital planetarium.
            </p>
          </article>

          <article>
            <h3>Three connected models</h3>
            <p>
              The project produced a localised model of student engagement, a
              localised model of contextual influence and MCDiP as their
              theoretically supplemented synthesis.
            </p>
          </article>

          <article>
            <h3>Peer-reviewed dissemination</h3>
            <p>
              The student-engagement strand was presented in the 2023 Physics
              Education Research Conference proceedings, providing a concise
              account of the attentiveness result and its empirical basis.
            </p>
          </article>

          <article>
            <h3>Practical design language</h3>
            <p>
              The distinction between primary, secondary and tertiary aspects
              gives instructors a way to explain why a planetarium is being
              used, what can be deliberately shaped and what must be managed.
            </p>
          </article>
        </div>
      </section>

      <section class="project-detail-section" aria-labelledby="limits-title">
        <div class="section-label">
          <p class="eyebrow">Limitations and future work</p>
          <h2 id="limits-title">A basis for further research, not a universal recipe.</h2>
        </div>

        <div class="project-prose">
          <p>
            The empirical study was limited to one digital planetarium, one
            university astronomy course and one cohort followed across two
            visits. Its models are grounded in that formal educational context
            and should not be assumed to transfer unchanged to public shows,
            school outreach, other disciplines or different planetarium designs.
          </p>

          <p>
            I therefore presented MCDiP as the basis for a formal grounded
            theory rather than a finished universal framework. Further work
            should test and refine it across additional cohorts, instructors,
            facilities and subjects; examine cognitive load more directly; and
            build shared evidence about which immersive affordances genuinely
            support particular instructional goals.
          </p>
        </div>
      </section>

      <section class="project-detail-section" aria-labelledby="collaboration-title">
        <div class="section-label">
          <p class="eyebrow">Collaboration</p>
          <h2 id="collaboration-title">Research across education, astronomy and visualisation.</h2>
        </div>

        <div class="project-prose">
          <p>
            The PhD was carried out in the University of Cape Town Department
            of Astronomy under the supervision of Prof Saalih Allie, with Prof
            Thomas Jarrett and Dr Lucia Marchetti as co-supervisors. The UCT
            Physics & Astronomy Education Research group contributed to study
            administration, instrument refinement and collaborative analysis.
            Show development also drew on the IDIA/UCT Visualisation Laboratory,
            with the student visits conducted at the Iziko Planetarium & Digital
            Dome.
          </p>
        </div>
      </section>

      <section class="project-resources" aria-labelledby="resources-title">
        <div>
          <p class="eyebrow">Resources</p>
          <h2 id="resources-title">Thesis and related publication.</h2>
        </div>

        <div class="resource-list">
          {
            project.links.map((link) => (
              <a
                href={link.href}
                target="_blank"
                rel="noopener noreferrer"
              >
                <span class="resource-type">{link.type}</span>
                <strong>{link.label}</strong>
                {link.description && <span>{link.description}</span>}
              </a>
            ))
          }
        </div>
      </section>

      <nav class="project-footer-nav" aria-label="Project navigation">
        <a href="/projects/">
          <span aria-hidden="true">←</span>
          Back to all projects
        </a>
      </nav>
    </div>
  </article>
</BaseLayout>

<style>
  .project-page {
    padding-bottom: clamp(4rem, 8vw, 7rem);
  }

  .project-breadcrumb {
    display: flex;
    align-items: center;
    gap: 0.65rem;
    padding-top: clamp(1.5rem, 4vw, 3rem);
    color: var(--text-muted);
    font-size: 0.88rem;
  }

  .project-breadcrumb a {
    text-underline-offset: 0.22em;
  }

  .project-detail-hero {
    display: grid;
    grid-template-columns: minmax(0, 1.25fr) minmax(280px, 0.75fr);
    gap: clamp(2rem, 6vw, 5rem);
    align-items: start;
    padding-block: clamp(3rem, 8vw, 7.5rem);
  }

  .project-detail-hero--without-media .project-detail-intro {
    grid-column: 1 / -1;
    max-width: 72rem;
  }

  .project-detail-hero--without-media .project-facts,
  .project-detail-hero--without-media .project-tags {
    grid-column: 1 / -1;
  }

  .project-detail-intro h1 {
    max-width: 17ch;
    margin-top: 0.8rem;
    font-size: clamp(2.9rem, 7.2vw, 6.4rem);
    letter-spacing: -0.06em;
    line-height: 0.94;
  }

  .project-deck {
    max-width: 49rem;
    margin: 1.6rem 0 0;
    color: var(--text-soft);
    font-size: clamp(1.15rem, 2.2vw, 1.5rem);
    line-height: 1.55;
  }

  .project-detail-intro .button-row {
    margin-top: 2rem;
  }

  .project-hero-media,
  .project-wide-media,
  .model-gallery figure {
    margin: 0;
    overflow: hidden;
    border: 1px solid var(--line);
    border-radius: 1.2rem;
    background: var(--surface);
  }

  .project-hero-media :global(.project-image) {
    width: 100%;
    aspect-ratio: 4 / 3;
  }

  .project-hero-media figcaption,
  .project-wide-media figcaption,
  .model-gallery figcaption {
    padding: 0.85rem 1rem 1rem;
    color: var(--text-muted);
    font-size: 0.82rem;
  }

  .project-facts {
    grid-column: 1 / -1;
    display: grid;
    grid-template-columns: repeat(5, minmax(0, 1fr));
    margin: 0;
    border-top: 1px solid var(--line);
    border-bottom: 1px solid var(--line);
  }

  .project-facts div {
    min-width: 0;
    padding: 1.1rem 1.1rem 1.15rem 0;
  }

  .project-facts div + div {
    padding-left: 1.1rem;
    border-left: 1px solid var(--line);
  }

  .project-facts dt {
    color: var(--text-muted);
    font-size: 0.72rem;
    font-weight: 750;
    letter-spacing: 0.1em;
    text-transform: uppercase;
  }

  .project-facts dd {
    margin: 0.3rem 0 0;
    color: var(--text);
    font-size: 0.94rem;
    line-height: 1.45;
  }

  .project-tags {
    grid-column: 1 / -1;
    display: flex;
    flex-wrap: wrap;
    gap: 0.55rem;
    margin: -1rem 0 0;
    padding: 0;
    list-style: none;
  }

  .project-tags li {
    padding: 0.42rem 0.72rem;
    border: 1px solid var(--line);
    border-radius: 999px;
    background: rgba(134, 184, 255, 0.06);
    color: var(--text-soft);
    font-size: 0.82rem;
  }

  .project-body {
    display: grid;
  }

  .project-detail-section {
    display: grid;
    grid-template-columns: minmax(190px, 0.42fr) minmax(0, 1.58fr);
    gap: clamp(2rem, 7vw, 7rem);
    padding-block: clamp(3.5rem, 8vw, 7rem);
    border-top: 1px solid var(--line);
  }

  .section-label h2 {
    max-width: 15ch;
    margin-top: 0.75rem;
    font-size: clamp(1.7rem, 3.2vw, 2.8rem);
    letter-spacing: -0.04em;
    line-height: 1.08;
  }

  .project-prose {
    max-width: 52rem;
  }

  .project-prose--wide {
    max-width: none;
  }

  .project-prose p {
    margin: 0;
    color: var(--text-soft);
    font-size: 1.04rem;
  }

  .project-prose p + p {
    margin-top: 1.35rem;
  }

  .project-prose .lead {
    color: var(--text);
    font-size: clamp(1.18rem, 2.3vw, 1.55rem);
    line-height: 1.55;
  }

  .scope-list {
    display: grid;
    gap: 0.8rem;
    margin: 2rem 0 0;
    padding: 0;
    list-style: none;
  }

  .scope-list li {
    position: relative;
    padding: 1rem 1rem 1rem 2.6rem;
    border: 1px solid var(--line);
    border-radius: 0.8rem;
    background: var(--surface);
    color: var(--text-soft);
  }

  .scope-list li::before {
    position: absolute;
    top: 1rem;
    left: 1rem;
    content: "✓";
    color: var(--accent);
    font-weight: 800;
  }

  .system-grid {
    display: grid;
    grid-template-columns: repeat(3, minmax(0, 1fr));
    gap: 1rem;
  }

  .system-grid--two {
    grid-template-columns: repeat(2, minmax(0, 1fr));
  }

  .system-grid--spaced {
    margin-top: 2rem;
  }

  .system-card {
    min-height: 21rem;
    padding: clamp(1.35rem, 3vw, 2rem);
    border: 1px solid var(--line);
    border-radius: 1rem;
    background: var(--surface);
  }

  .system-card--compact {
    min-height: 0;
  }

  .system-number {
    margin: 0 0 3rem;
    color: var(--accent);
    font-family: ui-monospace, SFMono-Regular, Menlo, Monaco, Consolas,
      monospace;
    font-size: 0.8rem;
  }

  .system-card--compact .system-number {
    margin-bottom: 1.75rem;
  }

  .system-card h3 {
    font-size: 1.35rem;
    line-height: 1.2;
  }

  .system-card p:last-child {
    margin: 1rem 0 0;
    color: var(--text-soft);
  }

  .process-list {
    display: grid;
    gap: 0;
    margin: 0;
    padding: 0;
    counter-reset: process;
    list-style: none;
  }

  .process-list li {
    position: relative;
    display: grid;
    grid-template-columns: 3.5rem minmax(0, 1fr);
    gap: 1rem;
    padding: 0 0 2.25rem;
    counter-increment: process;
  }

  .process-list li::before {
    width: 2.8rem;
    height: 2.8rem;
    display: grid;
    place-items: center;
    border: 1px solid var(--line-strong);
    border-radius: 50%;
    content: counter(process, decimal-leading-zero);
    color: var(--accent);
    font-family: ui-monospace, SFMono-Regular, Menlo, Monaco, Consolas,
      monospace;
    font-size: 0.75rem;
  }

  .process-list li:not(:last-child)::after {
    position: absolute;
    top: 3rem;
    bottom: 0.2rem;
    left: 1.37rem;
    width: 1px;
    content: "";
    background: var(--line);
  }

  .process-list h3 {
    margin-top: 0.15rem;
    font-size: 1.22rem;
  }

  .process-list p {
    margin-top: 0.55rem;
  }

  .table-wrap {
    max-width: 100%;
    overflow-x: auto;
    border: 1px solid var(--line);
    border-radius: 0.9rem;
  }

  .table-wrap:focus-visible {
    outline: 3px solid var(--accent);
    outline-offset: 4px;
  }

  table {
    width: 100%;
    min-width: 58rem;
    border-collapse: collapse;
    background: var(--surface);
  }

  caption {
    padding: 1rem;
    border-bottom: 1px solid var(--line);
    color: var(--text-muted);
    font-size: 0.82rem;
    text-align: left;
  }

  th,
  td {
    padding: 0.9rem 1rem;
    border-bottom: 1px solid var(--line);
    color: var(--text-soft);
    text-align: left;
    vertical-align: top;
  }

  thead th {
    color: var(--text);
    font-size: 0.76rem;
    letter-spacing: 0.05em;
    text-transform: uppercase;
  }

  tbody th {
    color: var(--text);
  }

  tbody tr:last-child th,
  tbody tr:last-child td {
    border-bottom: 0;
  }

  .project-wide-media {
    max-width: 52rem;
    margin-top: 2rem;
  }

  .project-wide-media :global(.project-image) {
    width: 100%;
    aspect-ratio: 4 / 3;
  }

  .outcomes-grid {
    display: grid;
    grid-template-columns: repeat(2, minmax(0, 1fr));
    gap: 1rem;
  }

  .outcomes-grid--three {
    grid-template-columns: repeat(3, minmax(0, 1fr));
  }

  .outcomes-grid article {
    padding: clamp(1.35rem, 3vw, 2rem);
    border: 1px solid var(--line);
    border-radius: 1rem;
    background: var(--surface);
  }

  .outcomes-grid h3 {
    font-size: 1.18rem;
  }

  .outcomes-grid p {
    margin: 0.8rem 0 0;
    color: var(--text-soft);
  }

  .evidence-note {
    max-width: 52rem;
    margin-top: 1rem;
    padding: clamp(1.35rem, 3vw, 2rem);
    border: 1px solid var(--line-strong);
    border-radius: 1rem;
    background: rgba(134, 184, 255, 0.07);
  }

  .evidence-note h3 {
    margin-top: 0.65rem;
    font-size: 1.25rem;
  }

  .evidence-note p:last-child {
    margin: 0.8rem 0 0;
    color: var(--text-soft);
  }

  .model-gallery {
    display: grid;
    gap: 1rem;
  }

  .diagram-frame {
    display: grid;
    place-items: center;
    padding: clamp(0.75rem, 2vw, 1.5rem);
    background: #fff;
  }

  .diagram-frame :global(.project-image) {
    width: 100%;
    height: auto;
    max-height: 62rem;
    object-fit: contain;
  }

  .project-resources {
    display: grid;
    grid-template-columns: minmax(190px, 0.42fr) minmax(0, 1.58fr);
    gap: clamp(2rem, 7vw, 7rem);
    padding-block: clamp(3.5rem, 8vw, 7rem);
    border-top: 1px solid var(--line);
  }

  .project-resources h2 {
    max-width: 14ch;
    margin-top: 0.75rem;
    font-size: clamp(1.7rem, 3.2vw, 2.8rem);
    letter-spacing: -0.04em;
    line-height: 1.08;
  }

  .resource-list {
    display: grid;
    gap: 0.8rem;
  }

  .resource-list a {
    display: grid;
    gap: 0.38rem;
    padding: 1.2rem 1.3rem;
    border: 1px solid var(--line);
    border-radius: 0.9rem;
    background: var(--surface);
    text-decoration: none;
  }

  .resource-list a:hover {
    border-color: var(--line-strong);
  }

  .resource-type {
    color: var(--accent);
    font-size: 0.72rem;
    font-weight: 750;
    letter-spacing: 0.08em;
    text-transform: uppercase;
  }

  .resource-list strong {
    font-size: 1.05rem;
  }

  .resource-list a > span:last-child:not(.resource-type) {
    color: var(--text-muted);
    font-size: 0.9rem;
  }

  .project-footer-nav {
    padding-top: 2rem;
    border-top: 1px solid var(--line);
  }

  .project-footer-nav a {
    display: inline-flex;
    gap: 0.55rem;
    align-items: center;
    color: var(--text-soft);
    font-weight: 700;
    text-decoration: none;
  }

  .project-footer-nav a:hover {
    color: var(--text);
  }

  @media (max-width: 1080px) {
    .outcomes-grid--three {
      grid-template-columns: 1fr;
    }
  }

  @media (max-width: 980px) {
    .project-detail-hero {
      grid-template-columns: 1fr;
    }

    .project-detail-intro,
    .project-hero-media,
    .project-facts,
    .project-tags {
      grid-column: 1;
    }

    .project-detail-intro h1 {
      max-width: 15ch;
    }

    .project-facts {
      grid-template-columns: repeat(2, minmax(0, 1fr));
    }

    .project-facts div,
    .project-facts div + div {
      padding: 1rem 1rem 1rem 0;
      border-left: 0;
      border-bottom: 1px solid var(--line);
    }

    .project-facts div:nth-child(even) {
      padding-left: 1rem;
      border-left: 1px solid var(--line);
    }

    .project-facts div:last-child {
      grid-column: 1 / -1;
      border-bottom: 0;
    }

    .system-grid,
    .system-grid--two {
      grid-template-columns: 1fr;
    }

    .system-card {
      min-height: 0;
    }

    .system-number {
      margin-bottom: 1.75rem;
    }
  }

  @media (max-width: 760px) {
    .project-detail-section,
    .project-resources {
      grid-template-columns: 1fr;
      gap: 2rem;
    }

    .section-label h2,
    .project-resources h2 {
      max-width: 22ch;
    }

    .outcomes-grid {
      grid-template-columns: 1fr;
    }
  }

  @media (max-width: 560px) {
    .project-detail-intro h1 {
      font-size: clamp(2.55rem, 14vw, 4.2rem);
    }

    .project-facts {
      grid-template-columns: 1fr;
    }

    .project-facts div,
    .project-facts div + div,
    .project-facts div:nth-child(even) {
      padding: 0.9rem 0;
      border-left: 0;
      border-bottom: 1px solid var(--line);
    }

    .project-facts div:last-child {
      grid-column: auto;
    }

    .process-list li {
      grid-template-columns: 2.8rem minmax(0, 1fr);
      gap: 0.75rem;
    }

    .process-list li::before {
      width: 2.3rem;
      height: 2.3rem;
    }

    .process-list li:not(:last-child)::after {
      top: 2.5rem;
      left: 1.12rem;
    }
  }

  @media print {
    .project-breadcrumb,
    .project-detail-intro .button-row,
    .project-footer-nav {
      display: none;
    }

    .project-detail-hero,
    .project-detail-section,
    .project-resources {
      display: block;
      padding-block: 1.5rem;
    }

    .project-facts {
      display: block;
    }

    .project-facts div,
    .project-facts div + div {
      border: 0;
    }

    .system-grid,
    .outcomes-grid,
    .model-gallery {
      display: block;
    }

    .system-card,
    .outcomes-grid article,
    .model-gallery figure,
    .resource-list a {
      margin-bottom: 1rem;
      break-inside: avoid;
      background: transparent;
    }
  }
</style>
